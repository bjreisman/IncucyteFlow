% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcs_convert.R
\name{incucyte_to_fcs}
\alias{incucyte_to_fcs}
\title{Convert Incucyte data to FCS files}
\usage{
incucyte_to_fcs(
  data,
  output_dir = "fcs_output",
  group_by_cols = c("Wells", "h"),
  channels = NULL,
  channel_desc = NULL,
  fluor_channels = NULL,
  filename_prefix = "incucyte",
  add_spillover = TRUE
)
}
\arguments{
\item{data}{Tidied Incucyte data frame from \code{\link[=tidy_incucyte]{tidy_incucyte()}}}

\item{output_dir}{Directory to save FCS files (created if doesn't exist)}

\item{group_by_cols}{Columns to group by; one FCS file per unique combination}

\item{channels}{Vector of channel/column names to include in FCS files}

\item{channel_desc}{Named vector of channel descriptions from \code{\link[=create_channel_desc]{create_channel_desc()}}}

\item{fluor_channels}{Fluorescence channels for spillover matrix}

\item{filename_prefix}{Prefix for FCS filenames}

\item{add_spillover}{Logical; add identity spillover matrix to FCS files}
}
\value{
Character vector of created file paths
}
\description{
Converts tidied Incucyte data to FCS format files, creating one FCS file
per unique combination of grouping variables (default: Wells and timepoint).
}
\examples{
\dontrun{
fcs_files <- incucyte_to_fcs(
  data = tidy_data,
  output_dir = "fcs_output",
  group_by_cols = c("Wells", "h"),
  channel_desc = create_channel_desc(FLR1 = "GFP", FLR2 = "TMRM"),
  filename_prefix = "experiment1"
)
}
}
